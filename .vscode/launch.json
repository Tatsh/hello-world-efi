{
  "configurations": [
    {
      "name": "General tests",
      "program": "${workspaceFolder}/build/general_tests",
      "request": "launch",
      "cwd": "${workspaceFolder}/build",
      "type": "cppdbg"
    }
  ]
}
