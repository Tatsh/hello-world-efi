jobs:
  analyze:
    name: 'Analyze'
    permissions:
      actions: 'read'
      contents: 'read'
      security-events: 'write'
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Checkout repository'
        uses: 'actions/checkout@v6'
      - name: 'Initialize CodeQL'
        uses: 'github/codeql-action/init@v4'
        with:
          languages: '${{ matrix.language }}'
      - name: Install tools
        run: sudo apt-get update -y && sudo apt-get install -y genisoimage gnu-efi ninja-build
      - run: |
          cmake -G Ninja -DWITH_GOP=ON .
          cmake --build . --config Debug
      - name: 'Perform CodeQL Analysis'
        uses: 'github/codeql-action/analyze@v4'
        with:
          category: '/language:${{matrix.language}}'
    strategy:
      fail-fast: false
      matrix:
        language:
          - 'c-cpp'
name: 'CodeQL'
'on':
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'master'
  schedule:
    - cron: '15 4 * * 3'
